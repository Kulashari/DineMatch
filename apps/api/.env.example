# Comma-separated origins allowed to call this API from the browser.
DINEMATCH_ALLOWED_ORIGINS=http://localhost:5173

# Add these only when live provider/database adapters are implemented.
GOOGLE_MAPS_API_KEY=
DATABASE_URL=
